const HOME = {
    INDEX: '/home'
};

const SIGN_UP = {
    INDEX: '/sign-up'
};

const SIGN_IN = {
    INDEX: '/sign-in'
};

const PROFILE = {
    INDEX : "/profile"
}

const ONBOARDING = {
    INDEX : "/onboarding"
}

const MY_WORKOUTS = {
    INDEX : "/workout"
}

export { HOME, SIGN_UP, SIGN_IN, PROFILE, ONBOARDING, MY_WORKOUTS };