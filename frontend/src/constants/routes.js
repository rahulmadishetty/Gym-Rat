const HOME = {
    INDEX: '/home'
};

const SIGN_UP = {
    INDEX: '/sign-up'
};

const SIGN_IN = {
    INDEX: '/sign-in'
};

const PROFILE = {
    INDEX : "/profile"
}


export { HOME, SIGN_UP, SIGN_IN, PROFILE };